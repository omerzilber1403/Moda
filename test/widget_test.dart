// Basic smoke test for FitFlip app.
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FitFlip app smoke test', (WidgetTester tester) async {
    // Intentionally minimal — full app requires Riverpod + GoRouter setup.
    expect(true, isTrue);
  });
}
